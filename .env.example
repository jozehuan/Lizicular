# PRODUCTION:

# ============================================
# CONFIGURACIÓN GENERAL
# ============================================
DOMAIN=tudominio.com
ACME_EMAIL=tu-email@gmail.com

# Versión de la aplicación (usada en tags de Docker)
VERSION=v1.0.0

# Registry de Docker (déjalo vacío para usar imágenes locales)
# REGISTRY_URL=registry.tudominio.com/
REGISTRY_URL=

# ============================================
# URLs y CORS
# ============================================
# URL pública del frontend (lo que el usuario escribe en el navegador)
# Si usas una IP, debe ser http://<TU_IP_PUBLICA>
FRONTEND_URL=https://app.tudominio.com

# URL pública del backend
# Si usas una IP, debe ser http://<TU_IP_PUBLICA>:8000
BACKEND_URL=https://api.tudominio.com

# Orígenes permitidos para CORS (separados por coma)
# El origen del frontend DEBE estar aquí. Si usas IP, pon http://<TU_IP_PUBLICA>
ALLOWED_ORIGINS=https://app.tudominio.com

# ============================================
# POSTGRESQL
# ============================================
POSTGRES_USER=lizicular
POSTGRES_PASSWORD=GENERA_PASSWORD_SEGURO_AQUI
POSTGRES_DB=authdb
