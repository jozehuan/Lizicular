# DEVELOPEMENT ============================================
# Docker Compose Credentials
# ============================================
# These variables are used by docker-compose.yml to set up services.
# In a real production environment, these should be managed by a secret manager.

# PostgreSQL Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=contrasena_postgres
POSTGRES_DB=authdb
# Poner las mismas que anteriormente
DATABASE_URL=postgresql+asyncpg://postgres:contrasena_postgres@localhost:5432/authdb

# Redis Credentials
REDIS_PASSWORD=contrasena_redis
# Poner la misma que anteriormente
REDIS_URL=redis://:contrasena_redis@localhost:6379/0

# MongoDB Credentials
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=contrasena_mongodb
# Poner las mismas que anteriormente
MONGODB_URL=mongodb://root:contrasena_mongodb@localhost:27017
MONGODB_DB_NAME=lizicular_db

# JWT Configuration
# CRITICAL: Change this to a strong random secret in production
SECRET_KEY=poner_secret_key_aqui

# Application Settings
APP_ENV=development
DEBUG=True
SECURE_COOKIES=False

# ChatBot Configuration and API Keys
LANGFUSE_SECRET_KEY=sk-...
LANGFUSE_PUBLIC_KEY=pk-...
LANGFUSE_BASE_URL="https://cloud.langfuse.com"

AZURE_OPENAI_ENDPOINT=poner_url_azure_openai_aqui
AZURE_OPENAI_API_KEY=poner_azure_openai_api_key_aqui
OPENAI_API_KEY=poner_openai_api_key_aqui
OPENAI_API_VERSION="2025-01-01-preview" # por ejemplo